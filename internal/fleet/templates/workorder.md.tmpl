---
wo: {{.ID}}
title: {{.Title}}
status: OPEN
plan: PLAN.md
journeys_exempt: false
pieces:
  - id: 1
    title: <first piece>
    verify: <verify command>
    integrated: false
---

# {{.ID}} — {{.Title}}

> **Status:** OPEN · Owner directive: <fill in — who asked for what, verbatim>

## Plan section

<approach + dated decisions the implementation must honor>

## Pieces (each: verify → journal → integrate; corpus green required)

1. <piece> — verify: <command>
2. <piece> — verify: <command>

Front-matter is the machine-readable contract (schema v1, validated by
`./scripts/fleet check`); keep `status` and `integrated` true to reality —
the predicates report drift with exact fix commands.
