# ci/pipelines/fleethub.yaml
component: fleethub
language: go
build_block: scripts/blocks/03-pipeline.sh
manifests_dir: infra/k8s
gates_file: lifecycle/gates.yaml
targets:
  dev:
    mode: dryrun-client
  stage:
    mode: dryrun-client
  prod:
    mode: cluster-apply
    note: served behind the lab k3s tier; approvals reachable over HTTP
