---
wo: WO-10
title: arjun-hk end-to-end — built THROUGH the product, deployed via fleet ops
status: EXECUTED
plan: PLAN.md
pieces:
  - id: 1
    title: arjun-hk component (onboard + single-page Go site with dollarbucks menu + iOS/Safari/retina contract) + C15a/C15b
    verify: go vet + go build + C15a + C15b + full corpus
    integrated: true
  - id: 2
    title: gated promote built→dev→stage→prod (gates re-run; approvals per .fleet.yaml policy; owner directive journaled)
    verify: PROMOTED lines + approval files + journaled evidence
    integrated: true
  - id: 3
    title: fleet-ops deploy to hk-03-dev (image build, DNS arjun.hk, tunnel, monitoring) + live verification
    verify: HTTP 200 + content + gatus/dashboard evidence + ops doctor all clear
    integrated: true
  - id: 4
    title: master checklist + STOP for owner acceptance (+ docs, WORKING-NOTES disposal, commit+push)
    verify: checklist all items evidenced; corpus fail=0; check 6/6
    integrated: true
---

# WO-10 — arjun-hk end-to-end through the product

> **Status:** IN PROGRESS this session · Owner directive: goal message
> 2026-08-27/28 (WO-8 → WO-9 → WO-10; WO-10 ends with arjun.hk deployed
> and a STOP for owner acceptance). arjun-hk is the first real customer
> built THROUGH the product per PLAN.md decision 2026-08-27.

## Plan section (decisions, dated 2026-08-28)

- **Component**: `fleet onboard arjun-hk --kind=service` registers the
  SDLC component (ops/PROJECTS.yaml + ci/pipelines/arjun-hk.yaml +
  ops/state/deployments.yaml entry); the registry entry gains
  `port: 8080`. Source lives at `apps/arjun-hk` (own pinned module,
  stdlib-only, mirroring fleetctl/fleethub).
- **Site**: single-page Go HTTP service. Embedded (embed.FS) dollarbucks
  menu page; `/healthz` for the k8s probe. Contract surface (asserted by
  C15a against the BINARY over loopback, not just the source):
  iOS — `viewport-fit=cover`, `apple-mobile-web-app-capable`,
  `apple-mobile-webapp-status-bar-style` (translucent), `theme-color`,
  `env(safe-area-inset-*)` padding; Safari — system font stack
  (no webfonts), `-webkit-font-smoothing: antialiased`; retina — all
  artwork inline SVG (vector, resolution-independent) plus a
  `min-device-pixel-ratio: 2` media query refinement. No JS, no external
  fetches, stdlib-only.
- **Gated promote**: built → dev → stage → prod with gates re-run at
  promote time (C3c; C4a; C6c) per lifecycle/gates.yaml. Dev approval:
  `FLEET_ACTOR=agent` (middle hop auto-approvable). Prod approval:
  `FLEET_ACTOR=owner-via-agent` — allowed by .fleet.yaml policy
  (allowed_actors: [owner, owner-via-agent]); the authority is the
  owner's directive that WO-10 ends with arjun.hk deployed, journaled
  verbatim next to the approval.
- **Ops deployment**: `ops register arjun-hk --host arjun.hk --port 8080
  --repo fleet/apps/arjun-hk` in the hk-03-dev site registry; image built
  by kaniko from the repo path (multi-stage golang:1.27-alpine →
  busybox:1.36 runtime, static binary); `ops deploy` self-reconciles
  CNAME arjun.hk → tunnel, tunnel ingress, gatus/dashboard monitoring;
  `ops verify` + content check + `ops doctor` all clear.
- **Rollback readiness** is inherited (WO-9 live drill proved
  build→deploy→rollback→verify on this same site machinery); WO-10
  records the fact rather than re-drilling.
- **STOP**: after the checklist every item carries evidence (journal
  lines, command outputs). No further mutation until the owner accepts.

## Master checklist (each item evidenced before STOP)

| # | Item | Evidence (all measured 2026-08-28, journaled) |
|---|---|---|
| 1 | SDLC component onboarded, doctor ALL CLEAR | ONBOARDED component=arjun-hk; `fleet doctor` OK checked_components=3 issues=0 |
| 2 | Site source + contract tests green | C15a (19 asserts: served binary over loopback — iOS/Safari/retina/menu), C15b; corpus 37u/463a fail=0 |
| 3 | Pipeline build reproducible (block03) | C3c re-run at every promote hop (gate); build_app arjun-hk OK |
| 4 | Promoted built→dev→stage→prod through gates | PROMOTED ×3 (02:01:33 / 02:01:41 / 02:02:08); approvals dev=agent, prod=owner-via-agent; agent-refusal policy evidence journaled |
| 5 | Image built via kaniko from repo path | BUILT arjun-hk tag=2026.08.28-r021046 sha=022cd845… (multi-stage golang:1.27-alpine→busybox) |
| 6 | Deployed to hk-03-dev (fleet-managed site) | DEPLOYED arjun-hk; running image == deployed intent (doctor deployed/arjun-hk ok) |
| 7 | DNS arjun.hk: CNAME + tunnel ingress | dns: arjun.hk CNAME retargeted → b4ac6032…cfargotunnel.com; tunnel ingress synced; ops doctor all clear |
| 8 | Monitoring: gatus endpoint + dashboard state | MONITOR OK: gatus + dashboard synced (8 endpoints); CM data byte-parity machinery from WO-8 |
| 9 | Live: https://arjun.hk serves the menu | ops verify → HTTP 200 OK; curl content "prices in dollarbucks" |
| 10 | Rollback readiness | WO-9 live drill (deploy v1→v2→rollback→v1 verified) on the same site machinery; ops rollback available for arjun-hk |

## Standing guardrails

- Approvals only through ./fleet / fleethub contract; owner directive
  journaled verbatim at the prod approval.
- No secrets anywhere; neutral cwd + FLEET_ROOT for drills; corpus gate
  before every integrate.

## Measured results

| Piece | Verify | Result |
|---|---|---|
| 2 gated promote | PROMOTED lines + approvals | **PASS** — built→dev→stage→prod 02:01:33→02:02:08 with gates re-run at every hop; dev=agent, prod=owner-via-agent per .fleet.yaml; agent refusal on prod evidenced; owner directive journaled verbatim |
| 3 ops deploy + DNS + monitoring | live evidence | **PASS — arjun.hk LIVE.** kaniko multi-stage image (sha 022cd845…, tag 2026.08.28-r021046); DEPLOYED; CNAME arjun.hk retargeted to the lab tunnel (stale record fixed by ensure_cname); tunnel ingress synced; monitoring 8 endpoints; `ops verify` HTTP 200 + curl content check ("prices in dollarbucks"); ops doctor all clear. One in-cycle fix: kaniko build context must be workspace-RELATIVE (labWorkspaceHostPath) for repo-internal repos |
| 1 component + site + contracts | corpus 37u/463a | **PASS** — onboarded (registry+pipeline+state, port 8080); apps/arjun-hk (embedded dollarbucks menu page, healthz, stdlib-only); C15a drives the served binary over loopback (iOS/Safari/retina/menu all green); C15b wiring; three units' hardcoded component counts made structural |
