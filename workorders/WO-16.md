---
wo: WO-16
title: rich ops register + next engine over the full ship path + golden replay unit
status: EXECUTED
plan: PLAN.md
pieces:
  - id: 1
    title: "ops register exposes the full runtime surface: --probe-path --run-as-user --service-account --args... --mem REQ[:LIM] --cpu REQ[:LIM] --storage SIZE[:MOUNT] --mount-sub NAME:/PATH... --mount-host /SRC:/DST..."
    verify: tests/C17b_rich_register (canonical block + rendered pod spec)
    integrated: true
  - id: 2
    title: "next engine sequences the ops ladder: prod-stage components owe ops build (no build entry) then ops deploy (deployed != built); site-registry-less components skipped"
    verify: tests/C17a_ship_path_replay
    integrated: true
  - id: 3
    title: "C17a golden replay: a brand-new component walks onboard→promote dev→approve dev→promote stage→approve prod (agent REFUSED per actor policy; owner-via-agent proceeds)→promote prod→ops build→ops deploy→NEXT none, driven ONLY by what next prints"
    verify: tests/C17a_ship_path_replay (19 asserts)
    integrated: true
---

# WO-16 — Rich register + the determinism core

> Owner directive 2026-08-28: the agent must do it right every single time
> irrespective of model quality. The answer: `next` is a state machine over
> the files — same state, same guidance, zero routing decisions left to the
> model; refusals print exact fixes; the replay unit proves the walk.

## Measured results

| Piece | Verify | Result |
|---|---|---|
| 1 | C17b | **PASS** — 19 asserts; rich block persists through BOTH parsers; pod spec renders runAsUser/mem limits/sub+host mounts/serviceAccount/args/probe path/PVC |
| 2 | C17a | **PASS** — the ladder: cold none → onboard → promote/approve hops → ops build → ops deploy → none |
| 3 | C17a | **PASS** — prod approval refused for FLEET_ACTOR=agent (the law holds mid-replay); full walk driven exclusively by NEXT action lines |
| all | corpus | **PASS** — 42 units / 534 asserts fail=0 |
