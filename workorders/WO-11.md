---
wo: WO-11
title: docs — fresh Ubuntu install guide (opencode + openchamber, no-drama path)
status: EXECUTED
plan: PLAN.md
pieces:
  - id: 1
    title: README fresh-Ubuntu section (dedicated user, $HOME-scoped node/npm, opencode-ai, openchamber installer, gh, fleet clone, k8s tier note, anti-drama rules)
    verify: bash scripts/test.sh + ./scripts/fleet check
    integrated: true
---

# WO-11 — Fresh Ubuntu install documentation

> **Status:** IN PROGRESS this session · Owner directive 2026-08-28:
> acceptance of the WO-10 delivery ("Ok this is good") + "Document and
> commit everything and add a section in the readme of how to go about
> this in a fresh install of Linux ubuntu. Including installation of
> opencode and open chambers... no symlink dramas and other hoops... no
> permission castration that just keeps on stopping the flow of the
> agents."

## Plan section (decisions, dated 2026-08-28)

- The guide documents the MEASURED reference deployment (this box):
  dedicated uid-1000 user owning its HOME + one workspaces root; node via
  nvm with the npm global prefix inside `$HOME` (`~/.npm-global`);
  opencode-ai via npm; openchamber via its official installer
  (Node 22+, state in `~/.config/openchamber`, localhost by default,
  `startup enable`, cloudflare tunnel); gh as a static binary in
  `~/.local/bin`; fleet cloned into the workspaces root, `./install.sh`,
  corpus + check as the acceptance gate.
- The "rules that prevent the dramas" list is written from the recorded
  incidents: WO-9's non-root migration (no root-owned files ever again),
  the ambient-credential ban (rule 7 + C12d), the HOME-less-kubectl
  `.kube/` litter, and the one-node-manager rule.
- Docs-only workorder: no product code changes; corpus must stay green.

## Pieces

1. README section (above) + PLAN.md state update to ACCEPTED + journal.

## Measured results

| Piece | Verify | Result |
|---|---|---|
| 1 README guide | corpus + check | **PASS** — corpus 37u/463a fail=0; check 6/6 after commit; guide grounded in the measured reference deployment |
