---
wo: WO-20
title: backup engine (R2+restic) + mocks reconcile + safety rails + legacy purge
status: EXECUTED
plan: PLAN.md
pieces:
  - id: 1
    title: "backup engine: ops backup init/backup/restore/restore-secrets, R2 restic repo, quiesce Jobs, retention; C20a contracts + C20b real round-trip"
    verify: tests/C20a + go test TestResticRoundTrip
    integrated: true
  - id: 2
    title: "1edge-mocks reconcile: PR 1ed-ge#35 merged, rewrite/mocks.Dockerfile, ops update verb, build+deploy+verify, doctor ALL CLEAR"
    verify: journal + ops doctor
    integrated: true
  - id: 3
    title: "safety rails: quiesce_state.json pre-write + sweep on mutating verbs + ops recover; registry critical:true refusal (--live only); explicit-names-or---all scope"
    verify: C20c unit (kill mid-backup -> next run resumes; critical refusal)
    integrated: true
  - id: 4
    title: "legacy purge: C-legacy gate (no interpreted runtimes in repo/rendered specs/live estate) lands RED on python:3.12-alpine; dashboard renderer ported to Go; dashboard-render.py deleted; golden flipped; gate GREEN"
    verify: C20d unit red->green + dashboard 200
    integrated: true
  - id: 5
    title: "live trio backup to R2 (explicit names) + non-disruptive restore drill to staging"
    verify: restic snapshots + staging byte-compare + journal
    integrated: true
  - id: 6
    title: "bootstrap k3s + deny-rule lines; interpreter deny LIVE in the user opencode config; corpus interpreter-free (tests/lib/jsonq); vm-tier seed server ported to Go"
    verify: corpus + push
    integrated: true
---

# WO-20 — Insurance + safety rails + legacy purge

> Incident (journaled): blanket backup swept openchamber (hosts opencode);
> interrupted run left it scaled to 0 -> 502. Owner lost trust; directives:
> deterministic enforcement (no python ever, corpus-asserted), critical
> protection, explicit backup scope. OPENCHAMBER/OPENCODE SESSION STATE
> BACKUP REJECTED BY OWNER (rebuildable working state).

## Measured results

| Piece | Verify | Result |
|---|---|---|
| 1 | C20a/C20b | **PASS** — 19 asserts + real restic round-trip |
| 2 | live | **PASS** — mock.1ed.ge 200, doctor all clear |
| 3 | live | **PASS** — quiesce_state pre-write + sweep on mutating verbs; critical:true refusal (openchamber); explicit scope enforced; kill-test unit split out to WO-21 piece 1 |
| 4 | gate | **PASS** — C20d RED on python:3.12-alpine -> GREEN; Go renderer built 2026.08.28-r212549 + labrender sidecar swapped; golden monitor.json flipped; dashboard live 200. Close-out: gate hardened (repo-wide scans incl. tests/ + testdata/; vacuous-cwd bug fixed — scans now run from FLEET_ROOT) |
| 5 | live | **PASS** — trio + secrets in R2 (nzbdav 33.6MiB, 9 snapshots); restore drill 157 files / 2s, non-disruptive |
| 6 | corpus | **PASS** — bootstrap deny-rule lines (step 6c) + interpreter deny LIVE in ~/.config/opencode/opencode.jsonc; corpus interpreter-free via tests/lib/jsonq (stdlib Go); docs split out to WO-21 piece 2 |

## Amendment (2026-08-28 close-out, owner-directed)

Pieces 3-6 were verified live but left integrated:false at close, which
falsified P5 and stalled the `next` engine (corpus C17a caught it). The
delivered scope above is marked integrated:true; the two genuinely open
items — the deferred C20c kill-test unit and the RESTORE/GLOSSARY/
PROJECTS-GUIDE docs — are tracked in workorders/WO-21.md (OPEN). The
close-out also removed the last interpreted-runtime uses from the test
workflow (C12b/C12c/C12d/C17a fixture edits are plain sed/awk now; JSON
shape asserts run through tests/lib/jsonq; vm-tier seed server is Go).
